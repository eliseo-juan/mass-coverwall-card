{
  "name": "mass-coverwall-card",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "oxlint src/ tests/"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "happy-dom": "^14.0.0",
    "oxlint": "^0.9.0"
  }
}
